/**
 * Types for the revision notes dialog.
 */

/**
 * Result returned when dialog closes.
 */
export interface RevisionNotesDialogResult {
  /** The revision notes provided by the reviewer */
  notes: string;
}
